(ns clj-json-ld.flatten
  "
  Implement the Flattening operation as defined in the
  [Flattening Algorithm section](http://www.w3.org/TR/json-ld-api/#flattening-algorithm).
  ")

(defn flatten-it [input context options]
  "[]")